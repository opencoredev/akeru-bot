---
"@t3tools/web": patch
---

Talk about bots and conversations instead of a T3-style chat app. Settings returns to bots, the command palette searches bots, notifications say the bot is working, and SSH remotes use ~/.akeru instead of T3 Code's ~/.t3. Cursor is no longer listed as a current provider. Preview MCP, background service, worktree branches, project schema, telemetry, and environment discovery now use Akeru names, with T3 paths kept as compatibility aliases.
